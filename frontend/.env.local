# Local development only. Gitignored.
# Publishable key ID only — RAZORPAY_KEY_SECRET must never appear in any
# NEXT_PUBLIC_* variable, because those are inlined into the client bundle.
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_TSTjk1Pj1ncwPK
