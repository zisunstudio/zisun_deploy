fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
alembic==1.13.1
pydantic==2.7.1
pydantic-settings==2.2.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-multipart==0.0.9
python-dotenv==1.0.1

# Redis (async support built-in via redis.asyncio)
redis[hiredis]==5.0.4

# SMS / WhatsApp
twilio==9.1.0

# HTTP client (for external APIs)
httpx==0.27.0

# Retry / circuit-breaker
tenacity==8.3.0

# Monitoring
sentry-sdk[fastapi]==2.5.1

# Cloudflare R2 / S3 presigned URLs
boto3==1.34.84

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
anyio[trio]==4.4.0
fakeredis[aioredis]==2.23.2
